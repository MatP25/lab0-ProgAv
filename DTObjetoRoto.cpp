/*
 * DTObjetoRoto.cpp
 *
 *  Created on: Apr 8, 2024
 *      Author: mat
 */

#include "DTObjetoRoto.h"

#include <string>
#include "Objeto.h"

using namespace std;

DTObjetoRoto::DTObjetoRoto() {
	// constructor por defecto

}

DTObjetoRoto::~DTObjetoRoto() {
	// destructor por defecto
}

DTObjetoRoto::DTObjetoRoto(Objeto* ObjetoRoto) {
	/*
	this->NombreObjeto = ObjetoRoto->getNombre();

	if (ObjetoRoto->estadoToString() == true){ // pregunta si el objeto esta prestado, si lo esta guarda en el data value que si lo esta

		this->NombreNino = ObjetoRoto->prestadoA->getNombre(); // Obtiene el nombre del niño
		this->Prestado = true;  // Si esta asociado a un niño setea el valor a true, sino a false  no se si es redundante, si se inicializa por defecto en true es innecesario

	}else{
		this->Prestado = false;
	}
	*/
}

